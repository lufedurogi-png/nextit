NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
# Hosts extra permitidos para /api/download-media (coma). Ej. si las imágenes están en otro dominio que el API:
# DOWNLOAD_MEDIA_ALLOWED_HOSTS=api-red.viku.com.mx
# Mismo ID de cliente OAuth (Web) que GOOGLE_CLIENT_ID en la API — visible en el navegador.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
